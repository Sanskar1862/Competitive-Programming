#include <bits/stdc++.h>
using namespace std;

int main()
{
    int T;
    cin >> T;

    while (T--)
    {
        int N,W,w;
        cin>>N>>w>>W;
        for(int i=1;i<=N;i++)
        {
            cin
        }
    }
}